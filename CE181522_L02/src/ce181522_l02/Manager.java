package ce181522_l02;

/**
 * L02_Create a Java console program to manage Candidates of a company.
 *
 * The Manager class is responsible for managing the candidate management
 * system. It allows the user to create candidates of different types
 * (Experience, Fresher, Internship), search for candidates, and display
 * candidate names.
 *
 * @author Nguyen Minh Tam - CE181522
 */
public class Manager {

    // Instance of ManagerCandidate class to manage candidate-related operations
    ManagerCandidate manager = new ManagerCandidate();

    // Instance of CheckInputValue class to handle input validation
    CheckInputValue IO = new CheckInputValue();

    /**
     * Starts the candidate management system, displaying the main menu and
     * handling user choices for managing candidates.
     */
    public void star() {
        
    }
}
